declare module '@editorjs/embed' {
    // Define the types for the module's exports
    export interface Embed {
        // Type declarations for properties
        // language: string;
        // code: string;
    }

    // Export the type or types you want to use in your application
    // export function highlightCode(block: CodeBlock): string;
}